name: '♿ Gemini A11y Pull Request Review'

on:
  pull_request:
    types:
      - 'opened'

defaults:
  run:
    shell: 'bash'

jobs:
  a11y-review:
    runs-on: 'ubuntu-latest'
    timeout-minutes: 10
    permissions:
      contents: 'read'
      pull-requests: 'write'
    
    steps:
      - name: 'Mint identity token'
        id: 'mint_identity_token'
        if: |-
          ${{ vars.APP_ID }}
        uses: 'actions/create-github-app-token@v2'
        with:
          app-id: '${{ vars.APP_ID }}'
          private-key: '${{ secrets.APP_PRIVATE_KEY }}'
          permission-contents: 'read'
          permission-pull-requests: 'write'

      - name: 'Checkout repository'
        uses: 'actions/checkout@v4'
        with:
          fetch-depth: 0

      - name: 'Run Gemini A11y PR Review'
        uses: 'google-github-actions/run-gemini-cli@v0'
        env:
          GITHUB_TOKEN: '${{ steps.mint_identity_token.outputs.token || secrets.GITHUB_TOKEN || github.token }}'
          GEMINI_API_KEY: '${{ secrets.GEMINI_API_KEY }}'
        with:
          gemini_api_key: '${{ secrets.GEMINI_API_KEY }}'
          gemini_model: '${{ vars.GEMINI_MODEL }}'
          gemini_cli_version: 'latest'
          upload_artifacts: true
          workflow_name: 'gemini-a11y-review'
          settings: |-
            {
              "model": {
                "maxSessionTurns": 30
              },
              "mcpServers": {
                "github": {
                  "command": "docker",
                  "args": [
                    "-i",
                    "--rm",
                    "-e",
                    "GITHUB_PERSONAL_ACCESS_TOKEN",
                    "ghcr.io/github/github-mcp-server:v0.27.0"
                  ],
                  "includeTools": [
                    "add_comment_to_pending_review",
                    "pull_request_read",
                    "pull_request_reviews_read",
                    "pull_request_review_write"
                  ],
                  "env": {
                    "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
                  }
                }
              }
            }
          extensions: |
            [
              "https://github.com/gemini-cli-extensions/code-review"
            ]
          prompt: |
            You are an expert accessibility (a11y) auditor.
            
            Please review this pull request for WCAG 2.1 and 2.2 Level A and AA compliance.
            
            Focus on these critical accessibility issues:
            - Images without alt text
            - Form inputs without labels
            - Links without href attributes
            - Buttons without type attributes
            - Tables without proper headers
            - Clickable elements without keyboard support
            - Low contrast text (insufficient color contrast)
            - Missing ARIA labels for interactive elements
            - Empty headings
            - Multiple H1 tags on a single page
            - Required fields not indicated
            - Links that open in new tabs without warning
            
            For each violation found:
            1. Identify the exact file and line number
            2. Quote the problematic code
            3. Explain why it's an accessibility issue
            4. Provide a corrected code example
            
            Be thorough but concise. Prioritize critical issues that block users with disabilities.